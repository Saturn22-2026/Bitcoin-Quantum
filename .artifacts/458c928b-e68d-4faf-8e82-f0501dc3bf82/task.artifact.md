- [ ] Integrate `mldsa-native` into Bitcoin build system
    - [ ] Create `mldsa.cmake` for source compilation
    - [ ] Update `bitcoin/src/crypto/CMakeLists.txt`
- [ ] Update `CPubKey` in `pubkey.h` and `pubkey.cpp`
    - [ ] Update sizes and internal buffer
    - [ ] Replace `secp256k1` verification with ML-DSA
- [ ] Update `CKey` in `key.h` and `key.cpp`
    - [ ] Update sizes and internal buffer
    - [ ] Replace `secp256k1` key generation and signing with ML-DSA
- [ ] Verify implementation with unit tests
    - [ ] Run `key_tests`
    - [ ] Run `pubkey_tests`
